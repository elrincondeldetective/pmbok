option_settings:
  # --- INICIO DE LA CORRECCIÓN ---
  # Se utiliza una sintaxis más explícita y se eliminan las barras finales
  # para asegurar la compatibilidad con el proxy de la plataforma Docker.
  - namespace: aws:elasticbeanstalk:environment:proxy:staticfiles
    option_name: /static
    value: static
  # --- FIN DE LA CORRECCIÓN ---